<p style="color: green"><%= notice %></p>

<%= render @car %>

abke<%= button_to "Add to Cart", cart_items_path(car_id: @car.id), method: :post, class: "btn btn-success", form: { style: 'margin: 16px 0;' } %>

<div>
  <%= link_to "Edit this car", edit_car_path(@car) %> |
  <%= link_to "Back to cars", cars_path %>

  <%= button_to "Destroy this car", @car, method: :delete %>
</div>
